# Optional Extensions Router

Open the [optional extensions reference](../.agents/context/optional-extensions.md).
Keep weak dependencies out of core loading and preserve extension activation
boundaries.

For Makie changes, use the `plotting` test project and a focused
`plotting/*` selector. For REPL or display changes, use the focused
`general/interactiveutils` or display contract tests.

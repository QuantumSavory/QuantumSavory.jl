# Examples Router

Open the [examples workflow](../.agents/context/workflows/examples.md), then the
context page for the demonstrated subsystem. Examples are executable
documentation, not a second behavioral specification.

Prefer the mirrored `test/examples/*_tests.jl` selector over an interactive or
plotting script. Keep setup files reusable, seeded simulations reproducible, and
optional visualization dependencies in the examples environment.

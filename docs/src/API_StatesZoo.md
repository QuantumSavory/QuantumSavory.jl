# [Predefined Models of Quantum States](@id Predefined-Models-of-Quantum-States)

```@raw html
<style>
    .content table td {
        padding-top: 0 !important;
        padding-bottom: 0 !important;
    }
</style>
```

The submodule `QuantumSavory.StatesZoo` provides models for many frequently used quantum states.

## Family Metadata

Use [`state_family_schemas`](@ref) for the explicit, deterministic built-in
catalog:

```@example states-zoo-metadata
using QuantumSavory
using QuantumSavory.StatesZoo

map(schema -> (
    family=schema.family,
    normalization=schema.normalization,
), state_family_schemas())
```

Each [`StateFamilySchema`](@ref) contains ordered
[`StateParameterSchema`](@ref) values with stable names, real-valued types,
bounds, recommendations, and documentation. The older `stateparameters` and
`stateparametersrange` APIs remain available and are derived from these
schemas.

Normalization is explicit. `NormalizedState` families already
represent density matrices of trace one. `WeightedState` families
carry a success probability or rate-like weight in their trace.

```@example states-zoo-metadata
weighted = BarrettKokBellPairW(1, 1, 0, 1, 1)
result = normalized_state_and_weight(weighted)
(
    original_weight=result.weight,
    normalized_trace=tr(express(result.state)),
)
```

QuantumSavory never silently normalizes a weighted family.
[`normalized_state_and_weight`](@ref) is the opt-in boundary that returns both
the normalized symbolic state and the original weight. It rejects zero-weight
states because they cannot be normalized.

```@docs; canonical=false
StateNormalizationStyle
StateParameterSchema
StateFamilySchema
state_family_schema
state_family_schemas
state_normalization_style
state_weight
normalized_state_and_weight
```

## Autogenerated API list for `QuantumSavory.StatesZoo`

```@autodocs
Modules = [QuantumSavory.StatesZoo]
Private = false
```

## API list for the `QuantumSavory.StatesZoo.Genqo` states

Implemented as wrappers to the `Genqo.jl` package.

```@autodocs
Modules = [QuantumSavory.StatesZoo.Genqo]
Private = false
```
